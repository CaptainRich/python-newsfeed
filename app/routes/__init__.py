# Find the module named "home" in the current directory, import the "bp" object, and rename it as "home".
from .home import bp as home

# Find the module named "dashboard" in the current directory, import the "bp" object, and rename it as "dashboard".
from .dashboard import bp as dashboard
